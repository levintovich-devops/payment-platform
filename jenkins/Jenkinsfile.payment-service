@Library('platform-shared-library@debug/shared-library') _

pipeline {
    agent {
        label 'docker'
    }

    stages {
        stage('Shared Library Test') {
            steps {
                echo 'Payment Service Pipeline'

                script {
                    platformPipeline()
                }
            }
        }
    }
}
